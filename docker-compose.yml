services:
  postgres:
    image: postgres:16
    container_name: reconflow-postgres
    environment:
      POSTGRES_DB: reconflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"